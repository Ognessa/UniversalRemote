# :feature-signal-api

Navigation contract for the signal editor screen.

**Layer:** Feature API

Single interface: `SignalEditorScreenBuilder`, extends `ScreenBuilder`, fixes `routeName = "SignalEditor"`. No implementation here.

## Dependencies

| Module | Why |
|---|---|
| `:core` | `ScreenBuilder` base interface |

## Testing

No tests.