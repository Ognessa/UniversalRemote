# :feature-library-api

Navigation contract for the element library picker screen.

**Layer:** Feature API

Single interface: `EditorLibraryScreenBuilder`, extends `ScreenBuilder`, fixes `routeName = "EditorLibrary"`. No implementation here.

## Dependencies

| Module | Why |
|---|---|
| `:core` | `ScreenBuilder` base interface |

## Testing

No tests.