# :feature-bluetooth-api

Navigation contract for the Bluetooth device picker screen.

**Layer:** Feature API

Single interface: `DevicePickerScreenBuilder`, extends `ScreenBuilder`, initiates `routeName = "DevicePicker"`. No implementation here.

## Dependencies

| Module | Why |
|---|---|
| `:core` | `ScreenBuilder` base interface |

## Testing

No tests.